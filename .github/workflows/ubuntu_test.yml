name: Run gopherden on Ubuntu
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Run gopherden
      run: bash gopherden.sh
